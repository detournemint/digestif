test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Cloudflare R2 — S3-compatible, generous free tier (10GB storage, no egress fees)
# Set credentials via: bin/rails credentials:edit
# Add to Render env vars: CLOUDFLARE_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET
#
# cloudflare:
#   service: S3
#   access_key_id: <%= Rails.application.credentials.dig(:r2, :access_key_id) %>
#   secret_access_key: <%= Rails.application.credentials.dig(:r2, :secret_access_key) %>
#   endpoint: https://<%= Rails.application.credentials.dig(:r2, :account_id) %>.r2.cloudflarestorage.com
#   region: auto
#   bucket: <%= Rails.application.credentials.dig(:r2, :bucket) %>
